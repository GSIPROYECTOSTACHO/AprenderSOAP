<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="utng.edu.mx.aprendersoap">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Display" />
        <activity android:name=".SignUp" />
        <activity
            android:name=".PantallaOpciones"
            android:label="@string/title_activity_pantalla_opciones"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AcercaDe"
            android:label="@string/title_activity_acerca_de"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".TemarioCurso"
            android:label="@string/title_activity_temario_curso"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ModuloUno"
            android:label="@string/title_activity_modulo_uno"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Practica1" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".QuestionActivity" />
        <activity
            android:name=".GameMultiActivity"
            android:label="@string/title_activity_game_multi"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivityGame"
            android:label="@string/title_activity_main_activity_game"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".GameOverActivity"
            android:label="@string/title_activity_game_over"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MultiplayerActivity"
            android:label="@string/title_activity_multiplayer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ScoresActivity"
            android:label="@string/title_activity_scores"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".GameActivity"
            android:label="@string/title_activity_game"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Games"
            android:label="@string/title_activity_games"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".quiz.PreguntaActivity" />
        <activity android:name=".quiz.Resultado" />
        <activity android:name=".Grafica" />
        <activity
            android:name=".ModuloDos"
            android:label="@string/title_activity_modulo_dos"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ModuloTres"
            android:label="@string/title_activity_modulo_tres"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Correo" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".MainVideos"
            android:label="@string/title_activity_main_videos"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Video1" />
        <activity
            android:name=".Ayuda"
            android:label="@string/title_activity_ayuda"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SeleccionaTema"></activity>
        <activity android:name=".grafica.GraficaModUno"></activity>
        <activity android:name=".grafica.GraficaModTres"></activity>
    </application>

</manifest>
